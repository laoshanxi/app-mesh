---
Keycloak:
  auth_server_url: http://localhost:8080
  realm: appmesh-realm
  client_id: appmesh-client
  # Inject the secret via environment variable APPMESH_Keycloak_client_secret instead of
  # committing it. Leave empty for a public client.
  client_secret: ""
